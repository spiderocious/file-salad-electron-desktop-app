// Icon proxy. All icon imports across the renderer go through `@icons`, never
// `lucide-react` directly — so the icon set is swappable in this one file.
export {
  Salad,
  UploadCloud,
  Link as LinkIcon,
  Copy,
  Check,
  Clock,
  Settings,
  LogOut,
  HardDrive,
  Cloud,
  ChevronLeft,
  X,
  AlertCircle,
  Loader2,
} from 'lucide-react';
